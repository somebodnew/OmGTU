using System;
class HelloWorld {
  static void Main() {
    int N = Convert.ToInt32(Console.ReadLine()),count = N;
    for(int i = 2; Math.Pow(2,i)-1<=N; i++)count+= N / Convert.ToInt32(Math.Pow(2,i)-1);
    Console.WriteLine(count);
  }
}
